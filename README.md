# Henderson Solutions

Marketing site for Henderson Solutions — the marketing arm of Henderson Racing,
built around their sustainability story (solar-powered fabrication, recycling,
buy-local philanthropy).

Static site: no build step. `index.html` + `style.css`, deployed on Netlify
via GitHub pushes to `main`.

## Local preview

```sh
python3 -m http.server 8000
```
